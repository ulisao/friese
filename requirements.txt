# Friese — Backend (stack sección 1.1)
# Instalar con: pip install -r requirements.txt

# Core
Django==5.1.15
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1

# Base de datos (Supabase / PostgreSQL)
psycopg[binary]==3.3.4
dj-database-url==3.1.2

# Infra web
django-cors-headers==4.9.0
whitenoise==6.12.0
# Servidor WSGI de producción (runserver no sirve para deploy). Solo se usa en
# Railway; en desarrollo se sigue usando `manage.py runserver`.
gunicorn==23.0.0

# Entorno
python-dotenv==1.2.2

# Tareas periódicas (cierre automático de remitos vencidos)
django-crontab==0.7.1

# Almacenamiento (Cloudflare R2 vía S3) y emails (Resend)
boto3==1.43.67
resend==2.35.0

# QR de la invitación de operador (users/invites.py). Se usa el factory SVG, que
# es Python puro: NO requiere Pillow.
qrcode==8.2
