# Estado de la corrida: lleva credenciales de los usuarios temporales del smoke
# test (se borran en la fase `cleanup`, pero no tienen por qué quedar en git).
state.json
# Capturas de pantalla de la corrida: evidencia de un run puntual, no del repo.
shots/
__pycache__/

# Copia local de seguridad que baja `evidence_restore_real.py` antes de borrar la foto
# de prueba. El script la borra al terminar; queda solo si la corrida se cortó a la
# mitad, y en ese caso es una foto real de un cliente: no va a git.
.restore-backup/
